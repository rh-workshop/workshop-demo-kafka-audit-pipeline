<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.produbanco</groupId>
  <artifactId>kafka-logs-pipeline</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <properties>
    <maven.compiler.release>25</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Quarkus community (Maven Central), no el BOM productizado: el cliente no tiene
         suscripcion de Application Foundations. La 3.33 es la LTS vigente y la primera rama
         soportada que incluye JDK 25 (el soporte pleno llego en 3.31); la 3.20 que habia antes
         quedo EOL en la comunidad en marzo de 2026. -->
    <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
    <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
    <quarkus.platform.version>3.33.3.1</quarkus.platform.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>${quarkus.platform.group-id}</groupId>
        <artifactId>${quarkus.platform.artifact-id}</artifactId>
        <version>${quarkus.platform.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency><groupId>io.quarkus</groupId><artifactId>quarkus-arc</artifactId></dependency>
    <dependency><groupId>io.quarkus</groupId><artifactId>quarkus-kafka-client</artifactId></dependency>
    <!-- ManagedExecutor: hilos gestionados por el runtime, que sí participan del shutdown
         (un `new Thread()` es invisible a Quarkus y no se puede drenar al recibir SIGTERM). -->
    <dependency><groupId>io.quarkus</groupId><artifactId>quarkus-smallrye-context-propagation</artifactId></dependency>
    <!-- Health checks para las probes de Kubernetes: sin esto el pod parece sano aunque el
         bucle de consumo este colgado, que es justo el modo de fallo que este pipeline evita. -->
    <dependency><groupId>io.quarkus</groupId><artifactId>quarkus-smallrye-health</artifactId></dependency>
    <!-- Tests del cifrado, la compresión y el enmascarado sin necesidad de un broker. -->
    <dependency><groupId>io.quarkus</groupId><artifactId>quarkus-junit5</artifactId><scope>test</scope></dependency>
    <dependency><groupId>org.assertj</groupId><artifactId>assertj-core</artifactId><version>3.26.3</version><scope>test</scope></dependency>
    <!-- Formato OTLP con la libreria OFICIAL de OpenTelemetry: clases Protobuf generadas del
         mismo esquema OTLP que usa el productor .NET (interoperables). -->
    <dependency>
      <groupId>io.opentelemetry.proto</groupId>
      <artifactId>opentelemetry-proto</artifactId>
      <version>1.3.2-alpha</version>
    </dependency>
    <!-- La compresion ANTES de cifrar (§7) usa GZip del JDK (java.util.zip) -> sin librerias de
         terceros. La compresion Kafka de 'masked' usa el zstd que ya trae AMQ Streams. -->
  </dependencies>

  <build>
    <finalName>${project.artifactId}</finalName>
    <plugins>
      <!-- Falla con un mensaje claro si Maven corre sobre un JDK anterior al 25. Sin esto, la
           compilación pasa (usa release=25) pero surefire arranca su fork con el JDK de JAVA_HOME y
           muere con "class file version 69.0 ... recognizes up to 65.0", que no dice qué arreglar. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <id>exigir-jdk-25</id>
            <goals><goal>enforce</goal></goals>
            <configuration>
              <rules>
                <requireJavaVersion>
                  <version>[25,)</version>
                  <message>Este proyecto compila a Java 25: exporta JAVA_HOME apuntando a un JDK 25+ (p.ej. sdk use java 25-graalce).</message>
                </requireJavaVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>${quarkus.platform.group-id}</groupId>
        <artifactId>quarkus-maven-plugin</artifactId>
        <version>${quarkus.platform.version}</version>
        <extensions>true</extensions>
        <executions>
          <execution>
            <goals>
              <goal>build</goal>
              <goal>generate-code</goal>
              <goal>generate-code-tests</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <!-- Sin <repositories>: Quarkus community se sirve entero desde Maven Central. -->
</project>
