<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.produbanco</groupId>
  <artifactId>kafka-logs</artifactId>
  <version>1.0.0</version>
  <packaging>pom</packaging>

  <!-- Tres módulos para que el generador de datos ficticios NO viaje en la imagen de producción.
       El aislamiento es estructural: 'pipeline' ni siquiera compila contra 'demo-producer', así que
       no basta una variable de entorno para convertir un pod productivo en emisor de transacciones
       inventadas. Se verifica en CI: el jar de 'pipeline' no contiene ninguna clase 'dummy'. -->
  <modules>
    <module>core</module>
    <module>pipeline</module>
    <module>demo-producer</module>
  </modules>

  <properties>
    <maven.compiler.release>25</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Quarkus community (Maven Central), no el BOM productizado: el cliente no tiene
         suscripcion de Application Foundations. La 3.33 es la LTS vigente y la primera rama
         soportada que incluye JDK 25 (el soporte pleno llego en 3.31); la 3.20 que habia antes
         quedo EOL en la comunidad en marzo de 2026. -->
    <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
    <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
    <quarkus.platform.version>3.33.3.1</quarkus.platform.version>
    <assertj.version>3.26.3</assertj.version>
    <opentelemetry.proto.version>1.3.2-alpha</opentelemetry.proto.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>${quarkus.platform.group-id}</groupId>
        <artifactId>${quarkus.platform.artifact-id}</artifactId>
        <version>${quarkus.platform.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.produbanco</groupId>
        <artifactId>kafka-logs-core</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>org.assertj</groupId>
        <artifactId>assertj-core</artifactId>
        <version>${assertj.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>io.opentelemetry.proto</groupId>
        <artifactId>opentelemetry-proto</artifactId>
        <version>${opentelemetry.proto.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>${quarkus.platform.group-id}</groupId>
          <artifactId>quarkus-maven-plugin</artifactId>
          <version>${quarkus.platform.version}</version>
          <extensions>true</extensions>
          <executions>
            <execution>
              <goals>
                <goal>build</goal>
                <goal>generate-code</goal>
                <goal>generate-code-tests</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <!-- Falla con un mensaje claro si Maven corre sobre un JDK anterior al 25. Sin esto, la
           compilación pasa (usa release=25) pero surefire arranca su fork con el JDK de JAVA_HOME y
           muere con "class file version 69.0 ... recognizes up to 65.0", que no dice qué arreglar. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <id>exigir-jdk-25</id>
            <goals><goal>enforce</goal></goals>
            <configuration>
              <rules>
                <requireJavaVersion>
                  <version>[25,)</version>
                  <message>Este proyecto compila a Java 25: exporta JAVA_HOME apuntando a un JDK 25+ (p.ej. sdk use java 25-graalce).</message>
                </requireJavaVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <!-- Sin <repositories>: Quarkus community se sirve entero desde Maven Central. -->
</project>
